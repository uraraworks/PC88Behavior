# M3 需要プロファイル（第1回）

測定日: 2026-08-07
対象: 公式 N88-BASIC ROM（32KB）。ディスク無し、キーボード入力のみ。
測定結果の実体: `measurements/*.txt`（アドレスとアクセス種別のみ。ROM の内容は含まない）

---

## 結論

**N88-BASIC ROM 32KB のうち、実際に実行されたのは 6734 バイト = 20.6%。**
ここまでの条件では、まだ飽和していない。

| 条件 | 内容 | 実行された番地 | |
|---|---|---:|---:|
| idle-noinput | 入力なしで放置 | 2225 | 6.8% |
| b0-prompt | 起動時プロンプトに RETURN | 3093 | 9.4% |
| b1-print | `PRINT 12345` | 3743 | 11.4% |
| b2-for | `FOR I=1 TO 5:PRINT I:NEXT` | 4607 | 14.1% |
| b3-string | `A$="XY":PRINT A$+A$,LEN(A$)` | 4078 | 12.4% |
| b4-math | `PRINT SIN(1)+SQR(2)*3/7-4` | 4672 | 14.3% |
| b5-screen | `SCREEN 1:CLS:LINE(0,0)-(100,100)` | 4372 | 13.3% |
| b6-error | `FOOBAR 999`（構文エラー） | 3731 | 11.4% |
| b7-program | 3 行のプログラムを入力し `LIST` / `RUN` | 5458 | 16.7% |
| **和集合** | | **6734** | **20.6%** |

条件を足したときの新規分:

```
idle-noinput  累計 2225   新規 2225
b0-prompt     累計 3093   新規  868
b1-print      累計 3743   新規  650
b2-for        累計 4647   新規  904
b3-string     累計 4917   新規  270
b4-math       累計 5373   新規  456
b5-screen     累計 5888   新規  515
b6-error      累計 6083   新規  195
b7-program    累計 6734   新規  651
```

新規が 0 に張り付いていない。**まだ測り足りない。** ディスク入出力、
グラフィック各種、DATA/READ、ファイル操作などが未測定。

## 分かったこと

### アイドル起動は 60 フレームで飽和する

フレーム数を 60 / 300 / 600 / 1800 / 3600 と変えても、実行された番地は
2225 バイトから 1 バイトも増えなかった。ROM はキーボードを走査して待つ
ループに入るだけで、入力が無い限り新しい経路は踏まれない。

**測定は短くてよい。** ファジングの回転数を上げられる。
需要を広げるのはフレーム数ではなく入力の種類である。

### 触れられた I/O ポート

```
入力: 00-0B 30 31 32 40 51 70 71 F4 F8
出力: 30 31 32 34 35 40 50 51 53-5F 64 65 68 70 71 C1 C3 C8 CA E4 E6 EB F8
```

`00`〜`0B` はキーボードマトリクスの走査。`50`〜`5F` の連続は CRTC 系。
L3 の実装単位を切るときの手掛かりになる。

## 手順上の失敗を 2 つ踏んだ（どちらも画面を見て気づいた）

数字だけを見ていたら、どちらも「そういうものだ」と受け入れて先へ進んでいた。
`q88measure` が測定結果に**終了時のテキスト画面を必ず含める**ようにしたのは、
条件が意図どおりだったかを結果自身で検証できるようにするため。

### ① 打鍵が起動プロンプトに食われていた

最初の測定では BASIC のどの機能を試しても実行番地が 3159 バイトで揃った。
FOR ループと文字列処理と三角関数が同じ経路しか踏まないはずがない。
画面を見たらこうなっていた。

```
 1| How many files(0-15)? print 1
 2| NEC N-88 BASIC Version 2.3
 5| Ok
```

起動時の `How many files(0-15)?` プロンプトに全部食われていた。
BASIC が立ち上がる前に打ち終わっていたので、コマンドは 1 文字も届いていない。
→ `--type-at` / `--type` を複数回指定できるようにし、先に RETURN で
プロンプトを抜けてから打つようにした。

### ② シフトが要る記号が別の文字になっていた

プロンプトを抜けた後も、プログラムがこうなっていた。

```
10 for j-1 to 3      ← FOR J=1 のはずが '=' が '-' に
20 print j:j         ← J*J のはずが '*' が ':' に
run
Syntax error in 10
```

コアは KEY88 コードをそのまま受け取るので、ASCII をそのまま送ると
「そのキーの**非シフト側**の文字」が入る。PC-88（JIS 配列）では
`=` は `-` の、`*` は `:` のシフト側にある。
→ シフトが要る文字の対応表を持たせ、土台のキーと同じ区間だけ
`RETROK_LSHIFT` も押すようにした。

修正後は `run` → `1 !` / `4 !` / `9 !` / `Ok` まで正しく動いている。

> これは [[定数の一致は経路の証明にならない]] と同じ形の失敗である。
> 「キーコードは正しく送っている」ことと「意図した文字が入力された」ことは
> 別の話で、後者は末端（画面）を見ないと分からない。

## 次に測るべきもの

- [ ] ディスク起動（`N88_FE.D88` 等）。FDC 経路は丸ごと未測定
- [ ] `SAVE` / `LOAD` / `FILES` — ディスク入出力の BASIC 側
- [ ] グラフィックの各命令（`CIRCLE` `PAINT` `PSET` `GET/PUT`）
- [ ] `DATA` / `READ` / `RESTORE`、`DEF FN`、`ON GOTO`
- [ ] エラー各種（`RESUME`、`ON ERROR GOTO`）
- [ ] 新規が 0 に張り付くまで続ける。張り付いてから実装の話に移る

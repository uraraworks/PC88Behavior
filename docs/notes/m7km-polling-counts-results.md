# m7km: 結果以外の入力を潰す — ポーリング回数に合図はあるか — 結果

実施日: 2026-09-09

## 位置づけ

[m7kl](m7kl-polling-counts-preregistration.md)が事前登録した測定の結果で
ある。事前登録は測定開始前に単独コミット（`47bdff3`）してあり、本稿では
水準・指標・段階0・判定規則・線を後から動かしていない。`src/`・`tools/`は
変更していない。

**結論を先に書く: 主判定はPA2（全位置で同じ）である。**
**run内の位置1〜9すべてでsub側ポーリングの回数は18回ちょうどで、
ばらつきも位置差も無かった。打ち切りの合図はポーリング回数にも現れない。**

## 覆いの検査と段階0

| 検査 | 結果 |
|---|---|
| **覆いの検査** | **全水準・両方向・独立2回とも真** |
| 偽陽性が無いこと（同一水準の独立2回で位置ごとの回数の分布が一致） | **真**（3水準とも） |
| 検出力があること | **真**（O-E1は位置1で18回、M-E1は19回で相違） |

## 「伴うポーリング」の範囲（委譲先が決め、全位置で同じ決め方を使った）

> **直前のFDCコマンドの結果読み取り終了から、当該`READ DATA`の結果読み取り
> 終了までの区間にある、sub側`IN $FA`の件数。** 位置1の「直前」は
> 単位の先頭側（`SENSE DRIVE STATUS`）。9件目の直後は次の単位の先頭コマンド
> 発行までとする。

**範囲の決め方は事前登録で固定していなかった**（委譲先に決めさせ、
**全位置で同じ決め方を使うこと**だけを縛った）。**その旨を開示に書く。**

## 合格条件（11項目すべて通った）

1. 覆いの検査（両方向・全水準で真）／2. `recv_run_field_leak_selftest.sh`
全項目OK・rc=0／3. `analyze_error_exchange_shape_selftest.sh` rc=0／
4. 段階0／5. 決定論性（3水準とも独立2回が素の`cmp`でバイト一致）／
6. `disk#8`原本不変（複製どうしの`cmp`）／7. O-E1が`m7kk`・`m7ki`・`m7kg`と
全指標一致（**照合13指標**——送信単位数・内容基数・多重度・長さ分布・
**位置ごとの基数`[1,1,1,1,3,1]`**、受信単位数・内容基数・長さ分布、K、run数、
run長多重集合、READ総数、エラー真件数、対応表の全単射）／8. M-E1が過去稿と
全指標一致（対応不能・`[1,1,1,1,5,1]`を含む）／9. 実行水準数3・期待3で一致／
10. B:の自作D88が全水準で同一／11. `check_cleanroom.sh`全14項目OK。

rcは全6実行0。再試行・タイムアウトは無し。**到達指標は事前登録どおり
置いていない。**

## 主指標: 位置ごとのポーリング回数

**値は出していない。「何回読んだか」だけである。**

| 位置 | 最小 | 最大 | 相異なる値の個数 |
|---|---:|---:|---:|
| **1〜9（全位置で共通）** | **18** | **18** | **1** |
| 9件目の直後（方向転換まで） | 0 | 1 | 2 |

- O-E1とO-E3で完全に一致（12単位／24単位）。独立2回でも一致。
- M-E1（run長1なので位置1のみ）は**19回**で固定。

判定規則を当てると:

- **PA2（全位置で同じ）: 成立。**
- PA1（位置9だけ違う）: 不成立。**位置9も18回である。**
- PA3（位置とともに規則的に変わる）: 不成立（**変わらない**）。
- PA4（ばらつく）: 不成立（**ばらつきが無い**——最小＝最大＝18）。
- O2・O: 不成立（合格条件11項目すべて通過）。

**主判定はPA2である。**

## 総合の線（`m7kl`で先に引いたものをそのまま当てる）

> **PA2のとき——ポーリング回数にも合図が無い。1.58節の「動かない軸」の
> 一覧に1行増える。ただし「9は入力に依存しない」と断定はしない
> （時間・他のポートの状態が残る）。**

**これが成立した。** 1.58節の一覧に**「sub側ポーリング回数（位置1〜9で
一定）」**を足す。

**断定はしない。残っているのは時間と、他のポートの状態である。**

## 記述（判定ではない）

**言えること:**

- **位置1〜9のどこでも、sub側ポーリングの回数は同じ**（最小＝最大＝18で
  ばらつき0）。**9件目だけ多い／少ないということは無い。**
- **9件目の直後だけは0〜1回**で、ここは**次の単位へ移る境目**である。
- **混成では回数が違う**（19回）。**器具は回数の違いを拾える。**

**言えないこと:**

- **「9は入力に依存しない」とは断定しない**（時間・他のポートの状態が残る）。
- **因果の向きは、そもそも本稿では問えない**（PA2なので差が無い）。
- **ポーリングで読んだ値は見ていない。回数だけである。**
- **「18」という回数から、内部の待ち方を推測しない。**
- 条件・打鍵・ディスクを固定した1つの使い方しか見ていない。

## 「9」が依存しないもの（1.58節の一覧の更新版）

| 軸 | 結果 | 出所 |
|---|---|---|
| トラックあたりセクタ数（4・8・16・26） | 変わらない | `m7ji` |
| シリンダ数・ヘッド数 | 変わらない | `m7jg` |
| セクタ番号の起点 | 変わらない | `m7ji` |
| 失敗の原因（トラック不在／セクタ不在） | 変わらない | `m7jo` |
| 要求の入口（`FILES`／`LOAD`／打鍵2回） | 変わらない | `m7js` |
| 実行量（`--frames`を4倍） | 変わらない | `m7jm` |
| FDCの結果ステータス（公開ビット全体） | 9件とも同じ | `m7ki` |
| **sub側ポーリング回数** | **位置1〜9で一定（18回）** | **本稿** |

**8軸目が埋まった。動くのは今もサブROMの差し替えだけである。**

## 次に何を測れば絞れるか

1. **残った入力は「時間」と「他のポートの状態」である。**
   時間は`m7hb`のH3が失敗した相手なので、**時間差ではなく、
   時間を動かす介入**（打鍵時刻を変える。`m7ic`が`F=2200`でやった先例が
   ある）で「9が動くか」を見るのが筋である。
2. **他のポートの状態**——`$FB`や、まだ見ていないポートの読み書き回数を
   同じやり方（回数だけ）で位置ごとに見る。
3. 1と2が済んでも動かなければ、**1.58節に「9は測れる範囲のどの入力にも
   依存しない」と書ける**。**それでも「内部の固定値である」とは書かない**
   ——**測れる範囲の外は測っていないからである。**

## 開示

- **「伴うポーリング」の範囲の決め方を、事前登録で固定していなかった。**
  委譲先に決めさせ、**全位置で同じ決め方を使うこと**だけを縛って、
  決め方を報告させた（本稿に転記した）。**`m7jy`で「指標の定義を固定して
  いなかった」ために食い違いが出た反省を、今回は部分的にしか活かせていない。**
  **範囲の取り方が違えば回数の絶対値は変わりうる**が、**位置間の比較
  （全位置で同じか）は同じ決め方を全位置に使う限り成り立つ**ので、
  **主判定PA2はこの不備の影響を受けない。** 次に回数を測るときは
  **範囲の定義も事前登録に書く。**
- 覆いの検査を合格条件の筆頭に置き、全水準・両方向で真だった。
- 段階0は測定の前に実施し、通ってから進んだ。
- 合格条件7・8では照合した指標名を列挙させた（13指標）。
- **委譲先の報告に取り違えも値の漏れも無かった**（`m7kc`以降6回連続）。
- `disk#8`の原本確認は複製どうしの`cmp`で行い、ダイジェストの値を出して
  いない。
- 測定の実行と集計はSonnetのAgentへ委譲した。**委譲したのは実行と集計のみで、
  判定・解釈は親セッションが行った。** 事故・逸脱の申告は無し。
- 実行した水準数（3）を出力に載せ、期待値と一致することを確認した。
- 上記以外の手順逸脱・事故は無い。無言でSKIPした分岐は無い。

## 情報境界

公式ROMのバイト列、公式ROMの逆アセンブル、`private/`の内容は読んでいない・
出力していない。**公式ディスクのイメージを直接読んでもいない。**
**ポートから読んだ値は出していない**（**何回読んだかだけ**）。**送信・受信
バイト列の値、結果ステータスの生の値、イベントの時刻の値・時刻差、画面本文も
出していない。** 記録したのは公開μPD765コマンド種別名とその記号列、件数、
run長とその多重集合、要求パラメータの相異なる個数（基数）、送信・受信単位の
基数・記号列・多重度・長さの分布・位置ごとの基数・覆いの検査の真偽、記号
どうしの対応表と関数性・単射性の真偽、位置ごとの結果ステータスの公開ビット
復号名の集合、**位置ごとのポーリング回数の最小・最大・基数**、エラー真偽の
集計、SEEK件数、最後に記録されたframeと全イベント数、rcだけである。
**シリンダ値・セクタ番号・PCN値・データポート値は表示も転記もしていない。**
生ログ・画面レポート・ディスク複製・自作ディスク・混成ROM一式・使い捨て
スクリプトはリポジトリ外（scratchpad配下）に置き、コミットしない。

## 検証

`tools/check_cleanroom.sh`は全14項目OK。`git status`で`private/`由来の混入・
生ログ・ROM像・ディスク複製が無いことを確認した。`src/`・`tools/`は変更して
いない。

## 根拠リンク（`ls`で存在確認済み）

[m7kl](m7kl-polling-counts-preregistration.md)（本稿の事前登録）・
[m7ki](m7ki-inside-the-unit-results.md)（**本稿の出所**。MA2と「結果以外の
入力は潰していない」）・
[m7kk](m7kk-unit-internal-structure-results.md)（位置ごとの基数）・
[m7kg](m7kg-layer-attribution-results.md)（層の帰属）・
[m7kd](m7kd-full-coverage-exchange-preregistration.md)（単位の定義と覆いの検査）・
[m7jy](m7jy-request-run-pairing-results.md)（**指標の定義を固定しなかった
反省**の出所。本稿の開示に関係する）・
[m7ic](m7ic-late-typing-retry-results.md)（**打鍵時刻を動かした先例**。
次稿の候補）・
[m7hb](m7hb-consecutive-read-rule-results.md)（H3が時間差で失敗した記録）・
`docs/spec/l3-subrom.md` 1.46節・1.47節・1.48節・1.57節・**1.58節**・3節
（第164〜184版）・
`docs/notes/m6-fdc-ports.md`（`$FA`/`$FB`がsub内部のFDCポーリング）・
`tools/analyze_main_to_sub.py`・`tools/analyze_fdc_ports.py`・
`tools/analyze_record_boundaries.py`・`tools/analyze_write_path.py`・
`tools/compare_l3_entry_fdc.py`・`tools/count_fdc_commands_after_frame.py`・
`tools/lib_l3_measure.sh`・`tools/conform_l3.sh`・
`tools/check_l3_screen_output.py`・`tools/stage_disk_by_digest.sh`・
`tools/harness/frontend/main.c`・`tools/make_l3_testdisk.py`・
`tools/recv_run_field_leak_selftest.sh`・
`tools/analyze_error_exchange_shape_selftest.sh`・`tools/check_cleanroom.sh`。

# m7lz（2回目）: 器具を直したB:候補8本の実測で関門はすべて真になり、3章の3項を判定できた

実施日: 2026-09-14
事前登録（追補）: [m7lz-...-preregistration-addendum](m7lz-b-candidates-recheck-preregistration-addendum.md)
（1回目の事前登録・関門・指標・判定・数え方は
[m7lz-...-preregistration](m7lz-b-candidates-recheck-preregistration.md)のまま）
HEAD: `401835f`（追補コミット。**実装は変えていない**。自作subROMはHEADのまま、
生成器 sha256先頭 `4f0b85c0344b`、1回目と同一）。

## 結論

> 1回目（`db822b8`、[結果ノート](m7lz-b-candidates-recheck-results.md)）はG3（決定論性）が
> D1-W・D2-W・D7-Wで偽になり`gate_failed`だった。原因は`--rom-dir`がlibretroコアの
> SAVEディレクトリも兼ねており、ROMディレクトリを走ごとに使い回したため前走の
> `SAVE"2:TQ"`結果が`.srm`経由で次走のREADへ混入したことだった（追補の「1回目が
> gate_failedになった原因」節）。
>
> **今回は走ごとにROMディレクトリを新規に作り直し（同じ8候補×{R,W}×official/mixed
> 各2回＋G4陽性対照1回、計65走を事前登録どおり再度走らせた結果、関門G1〜G5が
> すべて真になった。** 器具が漏れを塞いだことは、D1-Wのofficial run1・run2で
> sub `IN $FB`列のSHA-256が一致し、かつその値が1回目のofficial run1（まっさら側）
> と同じ`7e16390c2aa3`であることで確認した（一致）。
>
> 判定は**J1（5485・5508）=`disk10_differs`、J2（5448）=`write_differs`、
> J3（読みの広がり）=`read_differs`**。いずれも原因は同一で、**D8（disk#10）の
> main `IN $FC`列のSHA-256がofficial・mixedで一致しない**（件数は一致）ことによる。
> これは1回目の記述節、および`m7lw`がUNREAD系腕で報告した1.58節の既知の分岐と
> 同じ形の食い違いで、FDCコマンド種別列自体はD8のR・Wどちらの腕でも全長195件で
> 完全一致した（1回目の記述と同じ）。D8以外の7候補は、読み腕R・書き腕Wとも
> E・S・W（書き腕のみ）がすべて真だった。

## 器具が漏れを塞いだことの確認（本測定の判定を見る前に実施）

| 項目 | 結果 |
|---|---|
| D1-W official run1・run2のsub `IN $FB`列SHA-256一致 | **真**（件数15881、両者同一） |
| その値が1回目official run1（まっさら側）の`7e16390c2aa3`と同じ | **真** |

## 関門

| 関門 | 結果 |
|---|---|
| G1 媒体（8ダイジェストがそれぞれちょうど1本の複製を作る） | **真** |
| G2 取りこぼし0件（全65走） | **真** |
| G3 決定論性（各腕・各側でrun1とrun2のI/Oログデータ行が一致） | **真**（16腕×2側=32組すべて一致。1回目に偽だった D1-W・D2-W・D7-Wの6組も今回は一致） |
| G4 陽性対照（D6の書き腕をbreak-drive-selector混成で1回。WRITEストリームは両側とも0件[未到達]で一致だったが、`compare_l3_entry_fdc.py --after-frame 700 --write-operation`の入口区間unit/head差件数が22件で「差を検出できる」を満たした。1回目と同じ値・同じ成立理由） | **真** |
| G5 実装不変（生成器sha=`4f0b85c0344b`、1回目と同一） | **真** |

判定器の区別力の確認（E比較に1文字違いのSHAを渡すと偽になること）は、本測定の判定を見る前に確認済み（**偽**を返した）。

## 腕ごとの指標（E: main $FD/$FC一致、S: 画面署名一致、W: 書き腕のみ書き込みストリーム一致）

| 候補 | R: E | R: S | W: E | W: S | W: W | 備考 |
|---|---|---|---|---|---|---|
| D1 | 真 | 真 | 真 | 真 | 真（両側到達） | |
| D2 | 真 | 真 | 真 | 真 | 真（両側到達） | |
| D3 | 真 | 真 | 真 | 真 | 真（未到達＝両側0件） | |
| D4 | 真 | 真 | 真 | 真 | 真（未到達＝両側0件） | |
| D5 | 真 | 真 | 真 | 真 | 真（未到達＝両側0件） | |
| D6（disk#8） | 真 | 真 | 真 | 真 | 真（未到達＝両側0件） | |
| D7 | 真 | 真 | 真 | 真 | 真（両側到達） | |
| D8（disk#10） | **偽**(fd=真/fc=偽) | 真 | **偽**(fd=真/fc=偽) | 真 | 真（未到達＝両側0件） | main $FC のみ不一致 |

D8以外の7候補は全項目真。D8だけがmain `IN $FC`列のSHA-256不一致（件数は一致）でE偽になり、R・Wどちらの腕にも及ぶ。

## 判定

| 判定 | 結果 | 内訳 |
|---|---|---|
| J1（5485・5508、disk#10=D8） | **`disk10_differs`** | D8-RのE偽、D8-WのE偽により不成立 |
| J2（5448、WRITE経路） | **`write_differs`** | 8書き腕のうちD8-WのEが偽のため`w_all_true`不成立。参考: WRITEに届いた腕（disk#8・disk#10を除く6候補中）はD1・D2・D7の3候補（母集団の下限「3つ以上」は満たしていた） |
| J3（読みの広がり） | **`read_differs`** | diff_labels=[D8] |

## 記述として載せるもの（合否に使わない）

- FDCコマンド種別列: D8（disk#10）はR・Wどちらの腕でも全長195件で一致prefix195件・最初の差「なし（全長一致）」——1回目と同じで、第143・144版の「56件目で分岐」は現行では再現しなかった。D8以外の7候補も全腕でFDCコマンド種別列は公式・混成で全長一致（差なし）。
- official run1のREAD DATA発行件数（frame 700以降、入口区間）: 読み腕は候補により14〜117件（D8のみ117件、他は14〜16件）。書き腕は候補により14〜19件（D8は117件）。WRITE DATA発行件数: 書き腕D1・D2・D7が8件、他は0件（1回目と同じ内訳）。
- 終了時画面の行数・文字数（本文は書かない）: 読み腕は9〜19行・195〜891文字、書き腕は10〜11行・207〜229文字（候補ごとに幅があるが、officialとmixedは同一候補内で一致）。
- `.srm`の有無と大きさ（走後、記述のみ）: 全65走中、有（サイズ>0）だったのはD1-W・D2-W・D7-Wのofficial/mixed×run1/run2、計12走のみで、それ以外53走は`.srm`自体は生成されるがサイズ0だった。これはWRITEに届いた3候補の書き腕だけがSAVE差分を残すという読みと整合する（値・ファイル名は出していない）。
- G4のfdc要約: READ=公式16件・混成16件、WRITE=公式0件・混成0件、FDCコマンド種別列全長80件で一致prefix80件・最初の差なし、unit/head差件数22件（1回目と同一）。

## 事前登録の記述の読み方を決めた箇所

1回目の結果ノートと同じ（G3の比較にpath_event_sha、G4の「または」判定、J2の母集団の取り方）。今回変更なし。

## 言えないこと

- **エミュレータ（QUASI88 libretro版）上の振る舞いである。**
- 候補は手元の公式ディスク8本だけで、B:媒体一般には一般化しない。A:はdisk#8に固定した。
- 打鍵は`FILES 2`と`SAVE"2:TQ"`の2つだけである。
- 媒体の中身（ファイル一覧・データ）は書いていない。
- **D8（disk#10）のmain `IN $FC`不一致の原因（何が違うか）は、本測定の範囲では特定していない。** 件数は一致しており、`m7lw`が報告した1.58節の既知の分岐と同じ形だが、両者が同一原因かは確認していない。
- 1回目の記述節（`m7lz-b-candidates-recheck-results.md`）の値は上書きしていない。両方のノートを残す。
- 指示どおりにできなかった箇所: 特になし。事前登録（追補）どおりに器具だけを変え、腕・打鍵・フレーム・媒体・関門・指標・判定・数え方は1回目から変えずに実施できた。

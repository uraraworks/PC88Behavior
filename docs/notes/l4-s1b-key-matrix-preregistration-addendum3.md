# l4-s1b — 事前登録の追補3（SHIFT の文字コードの記録、測定前）

記録日: 2026-09-15
状態: 事前登録の追補、測定前

本体（`a8f7b05`）・追補（`d484cdb`）・追補2（`e678e59`）は書き換えず、SHIFT を重ねた腕で**記録する内容**だけを改める。問い・腕・関門・判定の名前と規則は追補2 のまま変えない。この追補3 が他に優先する。

## 経緯

追補2 による SHIFT の取り直し（結果 `a7911b7`）は、64腕を「変化なし18・別コード46・書かない0・未判定0」と判定したが、判定の件数だけを記録し、「別コード」となった腕の実際の文字コードを記録しなかった。本体の Q2 は「修飾を重ねたときに Q1 の文字コードがどう変わるか」を問うており、変わった先の値は Q2 の答えの一部である。生データは削除済みのため、同じ条件で測り直す。

## 記録する内容

- SHIFT を重ねた64腕それぞれについて、最下行（row0=19）を除いた差分のうち「前が空白だったセル」の文字コードを記録する（判定が「別コード」の腕は変わった先の値、「変化なし」の腕は Q1 と同じ値）
- 表は機械可読な形（TSV 等）で結果ノートに載せる: ポート・ビット・Q1 のコード・SHIFT を重ねたときのコード・判定
- 最下行の文字は、従来どおり値も位置も記録しない（`--count-only-rows 19`）

## 関門への追加

- G9 前回との一致: 今回の値から見た判定（Q1 と同じ値なら「変化なし」、違えば「別コード」）が、追補2 の結果（`a7911b7`）の判定と64腕すべてで一致する。一致しない腕があれば、その腕を列挙し、判定の食い違いとして記録する（値は今回のものを採る）

# L3 ディスク形式（公式形式の媒体）— 仕様書

第3版（2026-09-26）

## 0. この仕様書の位置づけ

自作ROM の `FILES`・`LOAD`・`SAVE` が**既存の N88-BASIC 形式の媒体**を扱えるように、
媒体上のデータの置き方を記す。2026-09-24、利用者の判断により「公式のディスク形式を扱う。
その形式は第三者の解析を読まず、自分で測る」と決めた（`docs/notes/m6f-a-directory-by-save-diff-preregistration.md` §0）。

**第1版の項目は、公式ROM に自分の入力（自作プログラム・自分で決めたファイル名）を与え、
参照 diskA の使い捨て複製の前後の差分から導出した。** 第2版で足した項目は、**自作の生成器で
作った空の媒体**を公式ROM にドライブ2で使わせ、その挙動と測定後の媒体から導出した（`m6f-c`）。 変わっていないバイト（公式ディスクの
既存の中身）は読んでいない。古い値は、セクタ内で一様かつ8個以上のときだけ使った
（境界の規則は `m6f-a` 事前登録 §1・追補2）。

**公式ディスク固有の事柄は書かない**（既存ファイルの数、空きの状況、割り当て番号の具体値）。

対象は 2D（40 シリンダ・2 ヘッド・16 セクタ／トラック・256 バイト／セクタ）の媒体である。

## 1. ディレクトリ

### 1.1 所在

**ディレクトリは (C=18, H=1, R=1) の先頭から始まる。** 空の媒体に最初に作ったファイルの
エントリは (18,1,1) の先頭に入る（`m6f-c` C8）。
**(18,1,3) もディレクトリのセクタである**（`m6f-a` D1、`m6f-b` G8）。

**ディレクトリは (18,1,1)〜(18,1,12) の 12 セクタである**（1セクタ 16 エントリ、計 192 エントリ）。
12 セクタそれぞれの先頭に置いたエントリを、公式ROM は名前で見つけた（`m6f-d` D6。ほかの枠を
削除済みのエントリで埋めた媒体）。

**探索は、未使用のエントリ（先頭 `0xFF`）に当たったところで止まる。** 前のセクタを未使用の
エントリのまま残すと、それより後ろのセクタのエントリは見つからなかった（`m6f-d` D6
`stops_at_unused`）。削除済みのエントリ（先頭 `0x00`）は飛ばして先へ進む。
**ディレクトリを読む側（`FILES` 等）は、(18,1,1) の先頭から順にエントリを読み、先頭 `0xFF` で
止め、先頭 `0x00` は飛ばす。**

### 1.2 エントリ

| 項目 | 値 | 根拠 |
|---|---|---|
| エントリ長 | **16 バイト** | m6f-b E3（2件の名前の位置の差） |
| 名前欄の位置 | エントリの**先頭**から | m6f-b E2 |
| 名前欄の長さ | **9 バイト** | m6f-b E4（4文字名と6文字名で、名前＋詰め物の長さが同じ） |
| 名前の詰め物 | **`0x20`**（空白） | m6f-b E4 |
| 名前欄の直後の1バイト | プログラム（`SAVE`）**`0x80`**、データファイル（`OPEN … FOR OUTPUT`）**`0x00`** | m6f-b E8 |
| 削除されたエントリ | 先頭1バイトが **`0x00`**（残りは削除前のまま） | m6f-b E5 |
| 未使用のエントリ | **`0xFF`** で埋まっている | m6f-b E6 |
| 10 バイト目（0 始まり） | ファイルの**最初の割り当て単位の番号** | m6f-d D4（6腕で一致）・D5 |
| 11〜15 バイト目 | 作成直後は `0xFF`（意味は未確定） | m6f-c・m6f-d の観察 |

**未確定:** 11〜15 バイト目の意味。9バイトの名前欄が「名前＋拡張子」のように
分かれるかどうか。

### 1.3 名前の大文字・小文字

**ROM はファイル名を与えられたとおりに格納する。** 大文字の符号で与えれば大文字、
小文字の符号で与えれば小文字のまま入る（m6f-b E1 `case_stored_as_given`）。

**未確定:** 名前を探すとき（`LOAD`・`KILL` 等）に大文字・小文字を区別するか。

### 1.4 空きのエントリの使い方

削除したエントリの枠は、次にファイルを作るときに使い直される（`m6f-c` A5b。作ってすぐ
`KILL` を 400 回繰り返し、毎回同じ枠を使った）。

## 2. 割り当て表

**(18,1,14)・(18,1,15)・(18,1,16) の3セクタは、ファイルの作成に伴って3つとも同じ位置が
同じ値に変わる**（m6f-b E7。本体の中身を変えた3腕で変化が完全に一致し、大きいファイルで
より多く変わった）。

自作の空の媒体でも、3セクタは**変わった位置と新しい値がまったく同じ**だった（`m6f-c` C3）。

### 2.1 空きの印

**割り当て表の値 `0xFF` は「空き」である。** 3セクタの全バイトを同じ値 V にした空の媒体で
ファイルを作れたのは V=`0xFF` のときだけで、他の 255 値では `Disk full`（ERR 68）になった
（`m6f-c` C1、全値掃引）。

### 2.2 割り当て単位に対応しない位置

**割り当て表の位置 160 以降は、割り当て単位に対応しない。** 2D の媒体では単位は 160 個
（2.3）。ROM はファイルを書くときに位置 160 以降へ**不定の値**を書くことがある
（`m6f-c` §4）。**読む側はこの範囲の値を意味のあるものとして扱ってはならない。**
（その値は記録していない。`docs/notes/contamination-2026-09-25-fat-buffer-overrun-values.md`）

### 2.3 割り当て単位

**割り当て単位は 8 セクタ（半トラック）である。** 番号 k（0〜159）の単位は、線形番号
L = (C×2+H)×16+(R−1) が 8k〜8k+7 のセクタ（`m6f-d` D1）。割り当て表の位置 k が単位 k に対応する。

### 2.4 鎖と終端

- 単位 k の値 T[k] が **160 未満**なら、それはファイルの**次の単位の番号**である（`m6f-d` D2）。
- 最後の単位の値は **`0xC0` ＋ その単位で使ったセクタ数**（1〜8）である（`m6f-d` 追補1 D3'、
  新しい記録数の6腕で前向きに確認）。
- ファイルは、エントリの 10 バイト目の単位から T をたどり、最後の単位の使ったセクタ数ぶんまでを読む。
- この規則で組み直したデータファイルとプログラムを、公式ROM は `INPUT #`・`LOAD` で読めた
  （`m6f-d` D5。鎖を切ったものと先頭をずらしたものは読めなかった）。

### 2.5 使用中の値と予約

- **`0xFF` 以外の値はすべて「使用中」とみなされる**（割り当てのとき避けられる。`m6f-d` 追補1 D7'、
  0x00〜0xFE の全値）。
- **単位 74・75（ディレクトリ・書き込み禁止の印・割り当て表のあるトラック18のヘッド1）は、
  使用中の値にしておかなければならない。** 空きのままだと、媒体を満杯まで書いたときに
  ROM がそこへ割り当て、ディレクトリが壊れた。`0xA0` にしておくと壊れなかった（`m6f-d` D8）。
  予約に使う値は `0xA0`（160 以上で、終端 0xC1〜0xC8 と重ならない）とする。
- 単位 72・73（トラック18のヘッド0）は空きでよい（ROM は空の媒体で単位 72 から割り当て始め、
  問題は起きなかった。`m6f-c`・`m6f-d`）。
- 空の媒体では、ROM は単位 72 から番号の小さい方へ、2 つ取っては 2 つ飛ばす順で割り当てた
  （`m6f-c`・`m6f-d` の観察。読む側には関係しない）。

## 3. 媒体の書き込み禁止の印

**(18,1,13) は、媒体の書き込み禁止を決めるセクタである。** 空の媒体で (18,1,13) の全バイトを
同じ値 W にしたとき、書き込みが `File write protected`（ERR 61）にならないのは、**W の 0x10 の
ビットが 0 のとき**だった（`m6f-c` 追補3、全値掃引）。(18,1,1) の値では変わらない。
D88 ヘッダの書き込み保護とは別の、媒体の中身による印である。

**書き込めるデータディスクを作るときは、(18,1,13) を全バイト 0x00 にすれば足りる**
（`m6f-c` 本測定6回目の媒体）。

**未確定:** (18,1,13) のどのバイトが印か。(18,1,13) の他の役割。

## 4. 空のデータディスクの作り方（ドライブ2で受け入れを確かめたもの）

次の規則だけで作った 2D の媒体は、公式ROM がドライブ2で `FILES`・`OPEN`（書き込み・読み込み）・
`SAVE`・`LOAD` を行えた（`m6f-c` C2 `accepted`）:

- 割り当て表の3セクタ (18,1,14)・(18,1,15)・(18,1,16): 全バイト `0xFF`、**ただし位置 74・75 は `0xA0`**（2.5）。
- (18,1,13): 全バイト `0x00`。
- それ以外の全セクタ: 全バイト `0xFF`（ディレクトリは全エントリ未使用になる）。

（`m6f-c` で受け入れを確かめた媒体は位置 74・75 も `0xFF` だった。予約を足した媒体で満杯まで
書いてもディレクトリが壊れないことは `m6f-d` D8 で確かめた。）

**公式ROM で起動できる空の媒体は作らない（データディスクだけ）。** 起動用セクタ (0,0,1) を
読める値で埋めると、BASIC まで届く値（戻り命令の符号）でもディスクを使わない BASIC として
立ち上がる（`m6f-c` 追補1・本測定3回目）。(0,0,1) を読めない形（セクタ無し・CRC エラー・
削除マーク・単密度）ではどれも BASIC まで届かなかった（`m6f-d` D9）。これより先は ROM の
内部の入口を探すことに近づくので進めない（2026-09-25 利用者と決定）。

## 5. 改版履歴

| 版 | 日付 | 内容 | 根拠 |
|---|---|---|---|
| 第1版 | 2026-09-24 | ディレクトリのセクタ1つ、エントリの欄、大文字・小文字の扱い、割り当て表の所在 | `docs/notes/m6f-a-directory-by-save-diff-results.md`、`docs/notes/m6f-b-directory-entry-fields-results.md` |
| 第2版 | 2026-09-25 | ディレクトリの始まり、空きのエントリの使い直し、空きの印、位置160以降の扱い、書き込み禁止の印のセクタ、空のデータディスクの作り方 | `docs/notes/m6f-c-blank-disk-acceptance-results.md`、同 追補1〜3 |
| 第3版 | 2026-09-26 | ディレクトリの広がりと探索の止まり方、エントリの先頭単位の欄、割り当て単位・鎖・終端、使用中の値と予約、起動できる媒体は作らないこと | `docs/notes/m6f-d-disk-rules-results.md`、同 追補1 |

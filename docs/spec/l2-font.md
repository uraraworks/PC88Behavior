# L2 — フォント / 漢字ROM

仕様書 第6版 / 2026-08-07

`docs/spec/l1-ipl.md` の型を踏襲する。**実装者が見てよいのはこの文書から右側だけ**
（`CLAUDE.md` 情報の流れ）。この文書は上流 QUASI88 のソースコード（BSD-3、公式ROMとは
無関係）と、実際の測定ログから書かれている。`vendor/quasi88-libretro/src/font.h` の
データ行（`0x` の並ぶ行）、および公式 ROM のバイト列は一度も読んでいない。

## 改訂の記録

| 版 | 日付 | 誰が | 何を |
|---|---|---|---|
| 第1版 | 2026-08-07 | 仕様セッション | 起草。フォント経路の確定、すり替わり経路の洗い出し、実測、候補フォント調査 |
| 第2版 | 2026-08-07 | 仕様セッション（調査専任） | 半角ANKフォントの方針を「ネイティブ8x8のフリーフォント」に確定。unscii-8 を条件（8x8かつ半角カタカナ保有）を満たす候補として追加。美咲フォント4x8案は不採用として整理。東雲フォントのライセンスを一次情報（配布アーカイブ同梱 LICENSE）で確認したが、東雲はそもそも8x8を持たないため4節の対象からは変わらず除外 |
| 第3版 | 2026-08-07 | 仕様セッション | 実装が一巡したので、決まったことを仕様側へ書き戻した。**字形の出どころを3つに分けた**（英数記号=unscii-8 / 半角カナ63字=自作 / セミグラフィック=ルール生成）。適合条件に**画面ピクセルまでの検証**を追加。未解決を実測結果で整理し直した |
| 第6版 | 2026-08-07 | 仕様セッション | **漢字は L2 の対象ではないと判明した。** PC-88 に漢字を表示するハードウェアは無く、漢字ROMは I/O 経由のデータ供給源にすぎない。「漢字は縦2倍されるか」という問い自体が成立しなかった |
| 第5版 | 2026-08-07 | 仕様セッション | グラフィック文字65コードを埋めた（生成56／資料上空白8／読めず1）。**「未決定」を3区分に割った。** 資料から確信が持てず実装側で決めた割当を推定として明記 |
| 第4版 | 2026-08-07 | 仕様セッション | **4.2節の判定を覆した。** 半角カナ63字を自作から美咲フォントへ差し替え。「半角が4x8だから不採用」は、必要なのが「8x8のカタカナ」であることを見落とした誤判定だった。漢字の描画経路を未解決に追加 |

---

## 0. この文書の根拠

| | |
|---|---|
| コード | `vendor/quasi88-libretro/src/memory.c`（300〜530, 640〜700行） |
| コード | `vendor/quasi88-libretro/src/LIBRETRO/libretro.c`（449〜504, 560〜638行） |
| コード | `vendor/quasi88-libretro/src/crtcdmac.c`（660〜751行）、`crtcdmac.h` |
| コード | `vendor/quasi88-libretro/src/screen-vram*.h` |
| 測定 | `tools/measure.sh` を用いた ROM 探索ログ（`log_cb` 経由、グリフのバイト列は含まない） |
| 調査ノート | `docs/notes/m5-font-survey.md`（読んだファイルと手順の記録） |
| 先例 | `docs/notes/m1-quasi88-survey.md` 4.1/4.2（疑似BIOSの判断） |

第三者はコードを読み、`tools/measure.sh` を自分の ROM で実行すれば
2〜3節の事実を再導出できる。4節（フォント候補）はライセンス文書の
一次情報から確認した。

### 「観測」と「推定」の分け方（`l1-ipl.md` と同じ約束）

観測はコードを読んで機械的に確定する事実、または実測ログで確認した事実。
推定はこちらの解釈で、裏付け資料をまだ持っていない。**実装は観測に従う。**

---

## 1. 観測 — フォントメモリの構造

`memory.c:334-336` で `font_mem` / `font_mem2` / `font_mem3` はいずれも
`8*256*2 = 4096` バイト確保される。内訳は前半 2048 バイトが「通常フォント」、
後半 2048 バイトが「グラフィックフォント」で、`font.h` 側の3配列
（`built_in_font_ANK` / `built_in_font_ANH` / `built_in_font_graph`）が
いずれも `[8*256*1]`＝2048バイトの宣言であることと一致する
（配列の**宣言サイズ**だけを見た。中身のバイト列は見ていない）。

- **1 文字 = 8 バイト**（1 バイト = 1 ラスタ行、8 行ぶん＝8x8 の 1bpp ビットマップ）
- **256 文字ぶん**で 1 つの「面」（2048 バイト）を構成する
- コード `0x00-0xFF` が通常のANK文字、`0x100-0x1FF`（同じ面のオフセット
  `0x800-0xFFF`）がグラフィック文字。`get_font_gryph()` が
  `attr & ATTR_GRAPH` の有無でどちらを引くか決める（`crtcdmac.c:731-734`）
- 文字コードの並び順そのもの（JIS X0201 の可視部分がどこに来るか等）は
  **今回はコードから確定できていない**。半角ANK+半角カナで256種を
  埋める設計であることは配列サイズと用途コメント（`font.h:2`
  「セミグラフィック文字は内蔵文字を使用」等）から読めるが、
  コード⇔グリフの対応表そのものは font.h のデータ行を読まない限り
  確定できないため、**推定に留める**（8節）

### `font_type` / `font_loaded` / `use_built_in_font` の意味（観測）

| 変数 | 意味 | libretro版での実態 |
|---|---|---|
| `font_mem` / `font_mem2` / `font_mem3` | Font1/Font2/Font3 の実体（各4096バイト） | `font_mem` のみが実際に画面へ出る（下記） |
| `font_type`（0/1/2） | `font_rom`（実際に描画で参照するポインタ）を `font_mem`/`font_mem2`/`font_mem3` のどれにするかの選択 | **常に既定値 0 のまま。** 変更するのは `menu.c:1667`（設定メニュー）だけで、libretro 版はこの呼び出し元を持たない |
| `font_loaded` | ロードに成功したフォント種別をビットで記録（bit0=Font1, bit1=Font2, bit2=Font3） | セットはされるが、参照している箇所は見つからなかった（表示用ステータスの可能性はあるが未確認） |
| `use_built_in_font` | スタンドアロン版の設定項目「内蔵フォントを使う」 | `getconf.c:540-541` にのみ現れ、libretro 版では未使用（設定メニューが無いため） |
| `has_kanji_rom` | 漢字ROMの読み込み成否 | `memory.c` 側のロード経路でのみセットされる。**libretro.c 側の独自ロードはこの変数を一切参照しない**（2.3節） |

**結論（観測）**: `font_type` が libretro 版で切り替わることは無いため、
**実際に画面へ出るのは `font_mem`（Font1）だけ**であり、`font_mem2`・
`font_mem3`（＝ FONT2.ROM・FONT3.ROM を読み込んでも）は描画に一切使われない
死んだ経路になっている。

---

## 2. 観測 — 画面に出るまでの経路

`get_font_gryph()`（`crtcdmac.c:703-751`）が文字コード→グリフ変換の一点。
`font_rom`（`font_mem` を指す。1節）から該当する 8 バイトを取り出し、
`T_GRYPH`（`bit8 b[12]`）の `b[0]`〜`b[7]` にコピーする。`b[8]`〜`b[11]` は
0 初期化で、`ATTR_LOWER` 属性が立っているときだけ
`b[crtc_font_height-1]` に下線用のベタパターン（`0xff`）を焼く
（`crtcdmac.c:743`）。

`screen-vram.h` の描画ループがこの `T_GRYPH` を 1 ラスタラインずつ
消費して VRAM 相当のバッファへ書く。消費回数（`CHARA_LINES`）は
`screen-vram-base.h` で次のように定義される。

```
200LINE時   CHARA_LINES = 200 / ROWS
400LINE時   CHARA_LINES = 400 / ROWS
```

`docs/spec/l1-ipl.md` 第0節・第2節が確定させた測定対象
（80桁×20行・24kHz・400ライン）に当てはめると
**`CHARA_LINES = 400 / 20 = 20`**。1 文字セルは 20 ラスタラインを使う。

フォント側の消費は `IF_LINE200_OR_EVEN_LINE()` というマクロが制御する。
400ライン用の定義（`screen-vram-full.h:34` ほか）は

```c
#define IF_LINE200_OR_EVEN_LINE()  if ((k & 1) == 0)
```

すなわち **k（0〜19）が偶数のときだけ次の1バイトを読む**。
同じフォントバイトを連続する2ラインへそのまま出す、単純な縦2倍の
ライン倍化である（補間・拡大処理は無い）。

`k=0,2,4,...,18` の10回でフォントバイトを読むが、`T_GRYPH` に実際の
グリフが入っているのは `b[0]`〜`b[7]` の8バイトだけなので、
**上位8回ぶん（k=0〜14の偶数、ラスタ行0〜15）がグリフ本体、
残り2回ぶん（k=16,18、ラスタ行16〜19）は空白**（`ATTR_LOWER` が
立っているときだけラスタ行18〜19にアンダーラインが乗る）。

**結論（観測）— 1文字のセルサイズと配置**:

```
8x8 の 1bpp グリフを、20 ラスタラインの文字セルの
上位 16 ライン（8行 × 縦2倍）にそのまま描画し、
下位 4 ラインは空白（ATTR_LOWER 属性時のみ下2ラインに下線）。
```

フォント自体を8x10や8x20に拡大・生成し直す必要は無い。
**必要なのは 8x8 の 1bpp ビットマップ、256文字ぶん、それだけ。**

### 漢字ROMを使わずANKだけで動くか（観測）

`memory_allocate()`（上流の標準経路、`memory.c:403-527`）は、
`FONT.ROM` が無くても `has_kanji_rom` が真であれば漢字ROMの一部
（`kanji_rom[0][(1<<11)]`）を ANK フォント代わりに使うフォールバックを
持つ。漢字ROMも無ければ `built_in_font_ANK`（出所不明の内蔵データ）に
落ちる。**つまり上流のコードは「ANKだけで動く」ことを前提にしており、
漢字ROMは無くても動く**（L2 の対象は KANJI1/2.ROM ではなく
FONT.ROM／built-in の代替に絞ってよい）。

ただし libretro 版はこの `has_kanji_rom` 分岐を経由せず、独自の
ロードロジック（3節）で上書きするため、**上流の設計意図どおりには
動いていない**。

---

## 3. 観測 — 「黙ってすり替わる」経路

M1（`docs/notes/m1-quasi88-survey.md` 4.1/4.2）で扱った疑似BIOSと同じ
「ROMが揃わなくても黙って別のものが動く」構図が、フォント経路にも複数ある。
`docs/notes/m5-font-survey.md` 2.3節で洗い出した表をそのまま引く。

| 対象 | ROMファイルを見つけた場合 | 見つからなかった場合 |
|---|---|---|
| font_mem[0x000-0x7FF]（Font1 ANK。**実際に画面へ出る**） | **読み込んだ内容を使わず**、`kanji_rom[0]` の一部で上書き | `built_in_font_ANK`（出所不明） |
| font_mem[0x800-0xFFF]（Font1 グラフィック。**実際に画面へ出る**） | `built_in_font_graph`（出所不明）で上書き | **何もしない**（`malloc` 直後の未初期化メモリがそのまま残る） |
| font_mem2[0x000-0x7FF]（Font2。描画には使われない） | **読み込んだ内容を使わず**、`built_in_font_ANH`（出所不明）で上書き | 同じく `built_in_font_ANH` |
| font_mem2[0x800-0xFFF]（描画には使われない） | 読み込んだ内容を保持 | `built_in_font_graph`（出所不明） |
| font_mem3[0x000-0x7FF]（描画には使われない） | 読み込んだ内容を保持 | 0 埋め |
| font_mem3[0x800-0xFFF]（描画には使われない） | `built_in_font_graph`（出所不明）で上書き | 0 埋め |

**実際に画面へ出る2箇所（font_mem の前半・後半）は、どちらも
「ROMファイルの読み込みに成功したかどうかに関わらず」中身がすり替わる。**
これは「無ければ代替」という通常のフォールバックより一段悪い。
**ROMが揃っていても、libretro 版の二重ロードがそれを踏みつぶす。**

原因は、`libretro.c:559` が上流の標準ロード関数 `memory_allocate()`
（すでに `font_mem` を正しく埋める経路を含む）を呼んだ**直後に**、
`libretro.c` 独自のロードロジック（560〜638行、コメントに
「PC88Behavior 計測ハーネス」とある区画のすぐ後ろ）が同じ
`font_mem`／`font_mem2`／`font_mem3` を**もう一度**上書きすることにある。
`memory_allocate()` 側の結果は握りつぶされ、後から実行される
libretro.c 側だけが有効になる。

また、libretro.c 側は `has_kanji_rom` を一切参照せず、
`kanji_rom[0][(1<<11)]` の中身が実際に正しく読み込まれているかの
確認をしないまま使う（`libretro.c:602`）。KNJ1.ROM の読み込みに
失敗していても、それを検出せずにそのまま使う経路になっている
（`load_system_file` はファイルの発見と `open` の成否しか見ておらず、
`filestream_read` が実際に何バイト読めたかを確認していない。
`libretro.c:493`）。

### 3節の実測（グリフのバイト列を含まない）

`tools/measure.sh` は libretro の `log_cb`
（`RETRO_ENVIRONMENT_GET_LOG_INTERFACE`）をフロントエンド経由で
標準エラーに転送する。これを使い、**ROMファイル探索の成否だけ**を
確認した（採取したのはファイルパスと「見つかった／見つからなかった」の
別だけで、グリフのバイト列や漢字ROMの中身は一切出力・保存していない）。

このリポジトリの `private/rom` に対して実行した結果:

- `KANJI1.ROM` / `KANJI2.ROM` → 読み込み成功
- `FONT.ROM` / `FONT2.ROM` / `FONT3.ROM` → 3つとも「見つからない」

この条件で `memory_allocate()`（上流の標準経路）は
`has_kanji_rom == TRUE` のため `font_mem[0x000-0x7FF]` を
`kanji_rom[0][(1<<11)]` 由来のデータで埋めるはずだが、直後に走る
`libretro.c` 側の独自ロードが「`FONT.ROM` が見つからない」場合の
分岐として無条件に `built_in_font_ANK`（出所不明）で上書きする
（`libretro.c:606`）。**実在する漢字ROMを使えたはずの経路が、
二重ロードによって出所不明のフォントに差し替えられていることを、
実測ログで確認した。**

### すり替わり経路のまとめ

**画面に効く経路（font_mem）だけで2本**（ANK半分・グラフィック半分）、
**画面に効かない経路（font_mem2, font_mem3）を含めれば6本**、
すり替わりが起きる。加えて、KNJ1.ROM の読み込み成否を確認せずに
使う経路が1本。**合計 7 箇所。**

### 塞ぐ方針

L2 の実装（今回はやらない。次のマイルストーンの範囲）では、次を満たすこと。

1. **`libretro.c` 側の独自ロードを廃止し、`memory_allocate()`（またはその
   置き換え）の結果だけを唯一の経路にする。** 二重ロードそのものを無くす
2. **ROMファイルの読み込みに成功したら、その内容を一切上書きしない。**
   成否に関わらず固定データへ差し替える現状の分岐を消す
3. **ROMが見つからない場合の代替は、出所不明の `built_in_font_*` ではなく、
   L2 で採用する自由フォント（4節）に固定する**
4. **`filestream_read` の戻り値（実際に読めたバイト数）を確認し、
   期待サイズと一致しない場合は「読み込み失敗」として扱う。**
   現状はファイルが `open` できさえすれば成功扱いになる
5. **`has_kanji_rom` 相当のガードを、実際に使う直前で必ず確認する。**
   未確認のバッファ（`kanji_rom` が読み込めていない場合）をフォントとして
   使わない
6. どの供給源が有効になったかを、`font_loaded` 相当のフラグとログで
   外部から確認できるようにする（3節の実測で使った経路をそのまま
   実装の検証にも使えるようにする）

---

## 4. 使用するフォントの候補（第2版）

必要なのは **8x8 の 1bpp ビットマップ、256文字ぶん（半角ANK＋半角カナ相当）**
（1・2節で確定）。ライセンス条項は一次情報（配布元ページ・配布アーカイブ同梱の
ライセンスファイル）を確認したものだけを候補に入れる。

### 4.0 方針決定（利用者判断）

半角ANKは **ネイティブに 8x8 でデザインされたフリーフォント**を採用する。
美咲フォントの半角グリフ（4x8）を 8x8 セルへ引き伸ばす、または余白で
埋めて配置する案は**採らない**。

理由: 4x8 を単純に縦や横へ引き伸ばす、あるいは余白を足して 8x8 セルに
収めるだけでは、字面の太さ・字形バランスが実機の PC-8801 の見え方
（8x8 でデザインされたグリフを 20 ラスタラインの文字セル上位16ラインへ
縦2倍で出す。2節）と異なるものになる。**「8x8 として描かれた文字」が
必要であり、「4x8 を加工して 8x8 に見せかけたもの」では目的を満たさない。**
これにより第1版で有力候補としていた美咲フォント（4.2）は、本節の対象からは外れる。

#### 第3版の追記 — 出どころを3つに分けた（**採用フォントは1つではない**）

第2版は「採用フォントを1つ選ぶ」書き方をしていたが、実際にはコード域ごとに
出どころを分けるのが正しかった。**この分割そのものが仕様である。**

| コード域 | 出どころ | 理由 |
|---|---|---|
| `0x20`-`0x7D` 英数記号 | **unscii-8** | ネイティブ 8x8。PD/CC-0（4.1節） |
| `0x7E` オーバーライン | **ルール生成** | unscii-8 に `‾`（U+203E）が無い。横棒1本なので生成できる |
| `0xA1`-`0xDF` 半角カナ 63字 | **美咲フォント**（第4版で変更。4.2節） | 8x8 の全角カナがそのまま使える。7×7 設計で字間があり読みやすい |
| セミグラフィック | **ルール生成** | 幾何図形。`docs/PLAN.md`「データテーブルはルールから生成する」 |

> **第4版の変更。** 半角カナは第3版まで**自作**（ASCII アートで直書き）だった。
> 美咲フォントを「半角が 4x8 だから」と誤って除外していたためである（4.2節）。
> 手描き版は `git log` に残してある。**消していないのは、次に同じ誤判定を
> しないため**である。

**半角カナを unscii-8 から採らない理由。** unscii の README に、半角カナは
**MSX の 6x8 ROM フォントを参考に描き起こした**旨の記載がある。
作者は全体を PD と宣言しており、書体自体は原則非著作物なので、
**法的には問題にならない見込み**である。それでもここで自作に切り替えるのは、
このプロジェクトの主張が「法的にセーフ」ではなく
**「出自を説明する必要が無い構造」**のほうだからである
（`docs/PLAN.md` 第3節）。**一言余計な説明が要る素材を中核に置かない。**

自作したものは `src/l2_font/make_font_rom.py` に **8x8 の ASCII アートとして
直書き**してある。バイト列ではなく字形として読める形にしてあるのは、
レビューできることと、**描いた履歴が git に残ること**を優先したため。
`0x5C` は unscii-8 に `¥`（U+00A5）が実在するのでそのまま使う。

### 4.1 unscii-8 — 条件を満たす候補

- フォント名 / バージョン / 作者: **unscii-8**（unscii フォントファミリーの
  8x8 バリアント）／ 作者 viznut（Ville-Matias "Viznut" Heikkilä）
- 配布元（一次）: <http://viznut.fi/unscii/>（作者本人の公式ページ）、
  ソース・配布ファイルは GitHub <https://github.com/viznut/unscii>
  （作者本人のリポジトリ。README にライセンス条項あり）
- **8x8 か**: 確認済み。`README.md` に "The two main variants are unscii-8
  (8x8) and unscii-16 (8x16)" とあり、`src/katakana.txt` 内の各グリフも
  `8x8` 見出しの下に 8行×8桁のビットマップとして定義されている
- **半角カタカナの有無**: 確認済み。`src/katakana.txt`（GitHub リポジトリ
  内、コード生成前のソースデータ）に `U+FF61` から `U+FF9F` まで
  **63コードポイント全て**（JIS X0201 の半角カナ全域と一致）が 8x8 の
  グリフとして定義されている。ソースコメントには
  > "The half-width kana glyphs are derived from Japanese MSX 6x8 ROM font.
  > Thickness added to allow better blending."
  とあり、半角カナ部分は MSX の 6x8 ROM フォントを参考に viznut 本人が
  8x8 へ描き起こしたもの、という出自の記載がある（他社ROMのバイト列を
  そのまま複製したものではなく、参考にして新規に作図したとの説明）
- ライセンス条項（一次情報、`README.md` より引用）:
  > "Licensing: You can consider it Public Domain (or CC-0) except for the
  > files derived from or containing parts of Roman Czyborra's Unifont
  > project (unifont.hex, hex2bdf.pl, unscii-16-full.*) which fall under GPL."
  - `unscii-8` 系のファイル（`unscii-8.hex` 等）は Unifont 由来ファイルの
    除外リストに含まれない＝**パブリックドメイン（CC-0）扱い**
  - 再配布: 可（PD）。改変（ビットマップ抽出・別形式ROMイメージへの変換）:
    可（PD、"with or without modification" を明記する記述は unscii 本体には
    無いが PD 宣言自体が改変を制限しない）。商用利用: 可（PD）。
    表示義務（クレジット表記）: **PD 宣言のため法的な強制力を持つ表示義務はない**
    （ただし出典を残すのは望ましい）。成果物（生成した互換ROMバイナリ）の配布:
    **可**（原資料が PD のため、そこから派生したデータの配布を妨げる条項がない）
  - 確認日: 2026-08-07
- 入手元URL・ファイル名・サイズ・配布形式（GitHub `fontfiles/` ディレクトリ、
  2026-08-07 時点の内容を `api.github.com` 経由で確認）:
  | ファイル名 | 形式 | サイズ |
  |---|---|---|
  | `unscii-8.hex` | Unifont 互換の `.hex`（テキスト、コードポイント:16進ビットマップ の行形式） | 約73KB |
  | `unscii-8.bdf`（`.pcf` としても配布） | X11 BDF/PCF ビットマップフォント | 約294KB（.pcf） |
  | `unscii-8.ttf` | TrueType（ビットマップを埋め込んだアウトライン化） | 約245KB |
  | `unscii-8.otf` | OpenType | 約133KB |
  | `unscii-8.woff` | Web font | 約60KB |
  実装で 8x8 の生ビットマップを取り出すだけなら `unscii-8.hex`
  （テキスト形式でパースしやすい）が最も扱いやすい
- 懸念点:
  - 半角カナグリフが「MSX 6x8 ROM フォントを参考にした」との記載があり、
    参考にした対象ROM自体の権利関係までは今回追跡していない
    （viznut 本人の PD 宣言をそのまま信頼する形になる）
  - PC-8801 の ANK コード表（コード→文字の対応）と unscii-8 の
    Unicode コードポイントとの対応付けはこちらで作る必要がある
    （4.4節・7節の未決定事項として残る）
  - グラフィック文字（セミグラフィック、0x100-0x1FF 相当）は
    unscii-8 の対象外。別途用意が要る

### 4.2 美咲フォント（Misaki Font） — **半角カナ63字に採用**（第4版で覆した）

- 配布元: <https://littlelimit.net/misaki.htm>、
  <https://littlelimit.net/font.htm>（ライセンス）
- ライセンスは無条件で再配布・改変・商用利用可（第1版で確認済み、変更なし）
- 取得は `tools/fetch_misaki.sh`（BDF 版をバージョン固定＋SHA-256 検証）

#### 第3版までの判定は誤りだった。**その誤り方を残す**

第1〜3版はこう書いていた——「**半角(ANK)グリフは 4x8** であり、4.0 の方針
（ネイティブ8x8）に反するため不採用」。

ASCII について言えばこれは事実である。**しかし必要だったのは
「8x8 のカタカナのグリフ」であって、「半角と呼ばれているグリフ」ではない。**
美咲はそもそも 8x8 の漢字フォントであり、**全角側のかな・カタカナは 8x8**
で描かれている。そして PC-88 のテキストセルは**どの文字も 8 ドット幅**なので
（第2節）、寸法は一致する。

**呼び名（全角／半角）で切って、実際の寸法を確かめなかった**のが誤りの原因である。
「半角」という語はこの文脈で寸法を意味しない。**判定に使う語が、対象の
性質そのものを指しているかを確かめること。**

さらに悪いことに、この誤判定のせいで半角カナ63字を手描きすることになり、
その初稿は**利用者から「識字できない」という評価を受けた**。
美咲は 7×7 に収めて字間を作る設計、つまり**読みやすさのために作られた完成品**
だったので、差し替えで問題はそのまま解消した。

**誤判定を消さずに残すのは、次に同じ切り方をしないため**である
（`CLAUDE.md` コミット規律「行き止まりを `git reset` で消さない」）。

### 4.3 東雲フォント（Shinonome） — ライセンス一次確認済み、サイズ不一致で不採用

- 一次配布元 <http://openlab.ring.gr.jp/efont/shinonome/> は 2026-08-07
  時点で接続不能（DNS/接続エラー）。配布アーカイブそのものを保存している
  ミラー（例: GitHub `code4fukui/shinonome-font`）に同梱される `LICENSE`
  ファイルの内容を、一次配布物の写しとして確認した
- ライセンス条項（配布アーカイブ同梱 `LICENSE` より引用、原文は日本語）:
  > 「このアーカイブに含まれるすべてのフォントデータ、ドキュメント、スクリプ
  > ト類はすべて Public Domain で提供されています。」
  > 「自由な改造、他フォーマットへの変換、組込み、再配布を行うことができます。
  > 同時に、これらはすべて完全に無保証です。」
  （日本国内では著作権の完全放棄が法的に不可能なため、`AUTHORS` に列挙された
  作者が権利を行使しないと宣言することで実質的パブリックドメインとする、
  との補足あり）
  - 確認日: 2026-08-07。**第1版で「一次確認できていない」としていた点は解消**
- サイズの注意: 配布アーカイブの `README` によれば、収録フォントは
  **12・14・16・18 ドットのみ**（JISX0201 = ASCII+半角カナ を含む）で、
  **8 ドット版は存在しない。** ライセンスは問題なくとも 4.0 の方針
  （ネイティブ8x8）を満たさないため、今回は候補から外れる

### 4.4 k8x12 / k12x8（littlelimit.net） — サイズ不一致で不採用（第1版から変更なし）

- 配布元: <https://littlelimit.net/k8x12.htm>、<https://littlelimit.net/k12x8.htm>
- ライセンスは 4.2 と同じ littlelimit.net 共通ライセンスで問題なし
- 8x12 または 12x8 であり 8x8 ではないため、4.0 の方針を満たさない

### 4.5 その他に当たった候補（条件を満たさず不採用）

- **PixelMplus**（itouhiro、M+ FONT LICENSE）: 収録サイズは
  `PixelMplus10` / `PixelMplus12` のみで **8px 版が存在しない**
  （ビットマップを埋め込んだアウトラインTTFで、8x8ではない）。サイズ不一致で除外
- **misc-fixed（X11）**: JIS X0201 対応の派生（`*-jisx0201.1976-0` 系）が
  存在するが、確認できたサイズは 6x9・7x13・8x13 等で、**8x8 に一致する
  ものは見当たらなかった**（未確認、条件を満たすとは言えない）
- **k6x8**（Japanese bitmap font、JIS第一・第二水準漢字＋かな＋英数字）:
  幅が 6 ドットであり 8x8 ではないため対象外
- **8x8DotJPFont**（emutyworks、Arduboy向け）: 名称は 8x8 だが、由来は
  美咲フォントの変換（4x8 相当の半角を加工したもの）と見られ、4.0 の方針
  （ネイティブ8x8）に反する可能性が高い。ライセンス・変換方式ともに
  一次情報での確認は行っていない（**未確認**として保留、採用候補には入れない）

### 4.6 まとめと結論

**条件（8x8 かつ半角カタカナ保有）を一次情報で確認できたのは
unscii-8（4.1）のみ。** 美咲（4x8）・東雲（12/14/16/18ドットのみ、
ただしライセンスは今回一次確認できた）・k8x12系（8x12/12x8）は
いずれもサイズ不一致で対象外、他に検討した候補（PixelMplus・misc-fixed・
k6x8・8x8DotJPFont）もサイズ不一致または出自未確認のため候補に残らなかった。

**推奨: unscii-8。** 理由は (1) 8x8・半角カタカナ63文字全域を一次情報で
確認できた唯一の候補であること、(2) ライセンスがパブリックドメイン（CC-0）
相当で再配布・改変・商用利用・成果物配布のいずれも問題がないこと、
(3) `.hex` 形式はテキストベースでパースが容易で、ROMイメージ生成の
自動化と相性が良いこと。ただし採用の最終判断は利用者が行うこと
（本節はダウンロード・実装を行わず、判断材料の提示に留める）。

**未決定（実装時に決めること。8節）**:
- 半角カナの文字コード対応表（JIS X0201、PC-8801 の ANK コード表）と、
  unscii-8 の Unicode コードポイント（U+FF61-U+FF9F ほか）との対応付け
  （今回は「該当コードポイントが 8x8 で揃っている」ことの確認に留まり、
  対応表そのものは作っていない）
- グラフィック文字（0x100-0x1FF 相当）用データをどう用意するか
  （unscii-8 の対象外）

---

## 5. 適合条件（何をもって「L2 が正しく差し替わった」とするか）

**実装より先に決める。物差しなので実装に合わせて緩めない**
（`l1-ipl.md` 第6節と同じ原則）。

**公式ROM保有者でなくても検証できることを優先する。**

### ① ソース経路の条件（コードレビューで検証。ROM不要）

- 3節で数えた 7 箇所のすり替わり経路が、実装後のコードで
  **1箇所も残っていないこと**。具体的には:
  - ROMファイルの読み込みに成功した場合、その内容を上書きする分岐が
    存在しないこと
  - `filestream_read`（またはその置き換え）の戻り値を確認し、
    期待サイズと一致しない場合は「失敗」として扱っていること
  - フォント読み込みの経路が1本化されており、二重ロードで
    後勝ちになる構造が無いこと

### ② ROM非保有者でも検証できる条件（実測。ROM不要）

- **ROM無しで起動し、`font_loaded` 相当のフラグ（またはログ）で
  「どの供給源が有効になったか」を外部から確認できること。**
  3節の実測（`log_cb` 経由でファイル探索ログを見る）と同じ方法で、
  今度は「フォント供給源そのもの」を報告させる
- **ROM無しで表示された文字のビットパターンが、4節で採用した
  自由フォントのグリフと一致すること。** 自由フォントのデータは
  ライセンス上リポジトリに含めてよいので、既知の文字コード
  （例: `'A'`, `'0'`, 半角カナの一部）を画面に出し、
  VRAM相当のバッファから読み取ったビットパターンを、リポジトリに
  持つ採用フォントのデータと比較するテストで検証する。
  **これは公式ROMの有無に関係なく実行できる**

#### 第3版で追加 — **画面ピクセルまで見ること**

`font_mem`（フォントが載るバッファ）に届いたことは、**画面に出たことの
証明にならない。** バッファと画面の間には CRTC・DMAC・描画側の変換が
まだあり、そこで落ちても CRC32 は一致したままになる。
このプロジェクトは同じ型を何度も踏んでいる（定常状態を `pc` から
推し量ろうとしてやめた経緯＝`l1-ipl.md` 第3節、
[[feedback_measure_the_end_not_the_signal]]）。**末端はピクセルである。**

したがって適合条件②には次を含める。

1. 自作 IPL に文字コードを並べて書かせ、**実描画の結果を静止画として取り出す**
2. 取り出したピクセルを、**生成器とは別実装で組んだ期待ビットマップ**と比較する
3. **8x8 が 20 ラスタのセルの上位16ラインに縦2倍で出ていること、
   下4ラインが空白であること**をピクセルで確認する（第2節の裏取り）
4. **わざと1文字潰したら落ちること**を確認する。落ちない検査は検査ではない

比較の基準は**自作フォントの字形**であり、公式 ROM ではない。
ここで見ているのは「意図した字形が、意図した位置に、意図した拡大率で
出ているか」であって、実機に似ているかどうかではない。

### ③ ROM保有者だけが検証できる条件（任意。参考情報）

- 実在する `FONT.ROM` を用意した環境で、`font_mem[0x000-0x7FF]` が
  読み込んだファイルの内容と一致すること（ファイルの内容とメモリ上の
  内容を比較するだけで、値そのものをリポジトリやログに書き出さない）。
  **このテストの実行と合否判定はROM保有者の手元でのみ行い、
  結果（一致/不一致）だけを報告する**運用にする

①②が実装の必須条件、③は「せっかく本物のROMがあるなら壊れていないか
確認できる」という位置づけの任意条件であり、③が無くてもL2は完成とみなす
（③はROMを持たない第三者には原理的に実行できないため、必須条件にすると
「公式ROM保有者でなくても検証できる」という目的に反する）。

---

## 6. 実装要件（次のマイルストーンでやること。今回は仕様のみ）

1. `font_mem`（実際に画面へ出る唯一の経路。1節）を埋める処理を1本化する。
   `memory_allocate()` 相当と `libretro.c` 独自ロードの二重構造を解消する
2. ROM読み込み成功時は内容を上書きしない（3節の方針2）
3. ROM非保有時のフォールバックは、4節で採用した自由フォント（美咲または
   同等ライセンスのもの）を 8x8 セルへ変換したデータに固定する
4. 半角ANK（JIS X0201）のコード⇔グリフ対応表を作る
   （4.4節の未決定事項。生成規則が要る。手作業の表なら生成スクリプトを
   `tools/` に置く）
5. `font_mem2`・`font_mem3`（描画に使われない、1節）の扱いは今回の
   適合条件に含めない。**将来 `font_type` を切り替える機能を実装しない限り
   死んだ経路のままでよい**

---

## 7. 未解決 / 次に測るべきこと

### 第3版で片付いたもの

- [x] 半角ANK（JIS X0201）のコード表と unscii-8 のコードポイントの対応付け
  → `docs/notes/l2-code-assignment.md`。JIS X0201 の規格と
  N88-BASIC リファレンスマニュアルから起こした
- [x] **字形の出どころの決定**（第3版・4.0節）。
  英数記号 `0x20`-`0x7E` = unscii-8 ／ 半角カナ63字 `0xA1`-`0xDF` = **自作** ／
  セミグラフィックと `0x7E`（オーバーライン）= **ルール生成**
- [x] `0x7E` の扱い。unscii-8 に `‾`（U+203E）が無いためチルダで代用していたが、
  **横棒1本はルールから生成できる**ので生成側へ移した。
  JIS X0201 の `0x7E` はチルダではない。**代用は「無い」ことを隠す**
- [x] 画面ピクセルまでの検証（第5節②に追加）。
  `font_mem` に届くこと ≠ 画面に出ること

### 残っているもの

### 第5版で片付いたもの

- [x] **グラフィック文字 65 コード**（`0x80`-`0xA0`, `0xE0`-`0xFF`）
  → `PC-8801MA ユーザーズマニュアル.pdf` **p.234**（資料4 2節「キャラクタコード表」）
  から**割り当てだけ**を採り、字形はルール生成した。読んだページは
  `docs/notes/l2-graphic-codes.md` に記録してある。
  **点の配置は写していない**（第5節の線引き）

  | 区分 | 数 | 意味 |
  |---|---|---|
  | 生成した | **56** | 棒メータ15・十字1・罫線16・太字罫線4・対角三角形4・トランプ柄ほか8・×ハッチ1・**漢字7**（円年月日時分秒） |
  | **資料上そもそも空白と確定** | **8** | `0xF8`-`0xFF`。**未決定ではない。空白が正しい** |
  | 読めずに残した | **1** | `0xA0`。600dpi で読み直しても資料側に情報が無かった |

  **この3区分を混ぜないこと。** 一緒くたに「未決定20個」と数えていた時期があり、
  残作業が実際より大きく見えていた。逆に「全部埋まった」と書けば読めなかった
  1個が消える。**分けて数えるのは正確さの問題ではなく、次に何をするかが変わるため**である

- [x] 漢字 7 字（`0xF1`-`0xF7`）を「8x8 で漢字を作る手段が無い」として
  未決定にしていたのを撤回。**美咲フォントは 8x8 の漢字フォントであり、
  7 字とも `DWIDTH 8` で実在した。**
  4.2 節の誤判定（「美咲は半角が 4x8」）と**同じ型**である——
  どちらも**手元にある素材の性質を確かめずに「無い」と判断した**。
  2 度出たので型として記録する

### 推定（**資料からは決まらなかった。実装側で決めた**）

第0節の約束どおり、観測と分けて書く。**次に資料が見つかったら、ここは差し替わる。**

| コード | 資料から読めたこと | 実装側で決めたこと |
|---|---|---|
| `0x9C`-`0x9F` | 「直角コーナー4種の**丸角版が4つある**」という構成 | どの回転がどのコードか |
| `0xE4`-`0xE7` | 「対角の三角形が4種ある」という構成 | どの向きがどのコードか |

**構成（何種類あるか）は観測、割り当て（どれがどれか）は推定**である。
実機と食い違っても画面に出せば分かる種類の食い違いなので、
**測定で後から確定できる**（ゴールA では実害が無い）。

### 残っているもの

- [x] **半角カナ63字の判読性** → **美咲フォントへ差し替えて解消**（第4版・4.2節）。
  以下は自作していた頃の記録として残す。**問題の出どころは字形の腕前ではなく、
  素材の判定を誤ったこと**だった
- [~] （旧）自作した半角カナ63字の判読性。初稿である。
  画面まで出したうえでの評価は「判別はできるが読みやすくはない」。
  英数記号は unscii-8 という完成品を使っているので、並べると差が出る。
  8x8 でカナを描くこと自体が厳しい（実機でも `ヌ`/`メ` のように
  似る組はある）が、**それを言い訳にせず字形を詰める余地は残っている**。
  改善するときの注意: **実機の字面に寄せに行かないこと。**
  比べる相手は「読みやすさ」であって公式フォントではない（4.0節）。
  作業単位としては独立していて、`src/l2_font/make_font_rom.py` の
  `KANA_ART` を書き換えて `tools/verify_l2.sh` を通すだけで完結する
### 第6版 — 漢字は L2 の対象ではない（**問いの前提が誤っていた**）

第4版は「漢字（全角）の描画経路は未測定」を未解決に挙げ、
「漢字も縦2倍されるのか」を測るつもりだった。**測る対象が存在しなかった。**

**観測（`vendor/quasi88-libretro` のコードから確定）:**

- `get_font_gryph()`（`crtcdmac.c:703-751`。文字コード→字形の唯一の変換点）に
  **漢字ROMへの参照が1つも無い**
- 漢字ROMは **I/O ポート経由のデータ供給源にすぎない**。
  `OUT E8/E9` でアドレスを書き、`IN E8/E9` で 1 バイトずつ読む（第1水準）。
  第2水準は `EC/ED`（`pc88main.c:1178-1192, 1473-1482`）
- テキストのアトリビュートに**全角(2バイト)を示すビットが無い**。
  `text_attr_buf` も 1 桁 = 1 コードの構造で、全角セルを持てない

**結論**: **PC-88 に「漢字を表示するハードウェア」は無い。**
漢字表示は、ソフトウェアが漢字ROMからビットを読んで
**グラフィックVRAM へ自分で描く**ことで実現されている。
PC-98 のようなハードウェア漢字テキストとは別物である。

したがって:

- **「漢字は縦2倍されるか」という問いは成立しない。** ANK の縦2倍
  （第2節）は `get_font_gryph()` を通る文字にだけ適用される規則であり、
  漢字はその経路を通らない
- **漢字は L2（フォント差し替え）の対象ではない。**
  L2 が差し替えるのは `font_mem` に載る ANK であって、漢字ROMではない
- **漢字の「字形」を出すのは L3/L4 の仕事**である。どう描くか（何ラインぶん
  どこへ書くか）は ROM 内部の実装であり、**測って再構成する対象**になる

> **この節は、問いを立て違えた記録として残す。** 「未測定だから測ろう」と
> したが、正しくは「その機構があるかを先に確かめる」だった。
> **測定の前に、測る対象が存在するかを確かめる。**

### 「縦長」の正体（**推定。ここは測っていない**）

観測されているのは次の2つ。

- グラフィックVRAM は 400 ライン時、ラスタ位置ごとに別アドレスを読む
  （`screen-vram-400.h` の `src + k*80`）。**ハードウェアでの縦倍化は無い**
- コアが libretro に申告するアスペクト比は **`1.6` = 640/400**
  （`LIBRETRO/libretro.c:871`）。**正方形ピクセル相当で、CRT 補正は入っていない**

ここから先は推定である。実機の CRT は **4:3** なので、640x400 を映すと
ピクセルは横より約 **1.2 倍**縦長になる。200 ライン（640x200）なら約 **2.4 倍**。
16x16 の漢字を 200 ラインモードで描けば、ドットでは正方形でも
画面上は 16 × 32 相当に見える。**「PC-88 の漢字は縦長」という印象は
おそらくこれである。**

**そして、このエミュレータはそれを再現していない**（`1.6` = 正方形ピクセル）。
実機の見え方に寄せたいなら表示側の話であって、フォントデータの話ではない。
**フォントを縦長に作って辻褄を合わせてはいけない。**

- [ ] （旧・第4版）**漢字（全角）の描画経路は未測定。**「PC-88 は漢字が縦長」という
  印象があるが、この文書が確定させたのは **ANK（8x8）** の側だけである
  （第2節: 400ライン時は `IF_LINE200_OR_EVEN_LINE()` により
  **同じフォントバイトを2ラインへ出す縦2倍**。**200ライン時は倍化されない**ので、
  同じ 8x8 データがモードによって正方形にも縦長にもなる）。
  漢字ROM（KANJI1/2）から全角グリフを引く経路が同じ倍化を受けるのか、
  16x16 が 16 ラインに 1:1 で載るのかは**まだ測っていない**。
  さらに 640x400 を 4:3 のモニタに映すとピクセル自体が縦長になるため、
  **「ライン倍化」と「ピクセル比」の2要因を分けて測る必要がある**
- [ ] セミグラフィックコード（`0x100`-`0x1FF`）の実際の用途
  （BASIC 上のどの機能がこの文字コード域を使うか）は未調査
- [ ] `FONT.ROM` 実在時、`kanji_rom[0][(1<<11)]` 由来のデータと
  `FONT.ROM` 本来の内容が同一のものを指しているかどうか。
  私物の `FONT.ROM` サンプルが無く未確認（3節）
- [ ] `font_loaded` フラグが現状どこからも参照されていないことの
  確認（`grep` ベースの調査に留まる。呼び出しグラフ全体は未走査）
- [ ] unscii-8 の半角カナグリフが参考にしたという「MSX 6x8 ROM フォント」
  自体の権利関係は今回追跡していない（4.1節の懸念点）。viznut 本人の
  PD 宣言をそのまま信頼する形になっている点は残る
- [ ] misc-fixed（X11）に 8x8 の JIS X0201 対応版が存在するかどうかは
  「見当たらなかった」までの確認に留まり、存在しないと断定はしていない
  （4.5節）
- [ ] 8x8DotJPFont（emutyworks）の由来（美咲フォントの変換かどうか）と
  ライセンスは一次情報で確認していない（4.5節、未確認のまま保留）

# PC88Behavior — クリーンルーム規律

このファイルは開発メモではなく **プロセスの証拠** である。
リポジトリにコミットされていること自体が、「規律を主張した」ではなく
「規律を設定として持っていた」ことの記録になる。書き換えるときは履歴に残す。

設計と進め方: [docs/PLAN.md](docs/PLAN.md)

---

## 禁止事項（人間・エージェント共通、例外なし）

1. **公式 ROM のバイト列を読まない・出力しない・保存しない。**
   `xxd` / `od` / `hexdump` / `strings` を公式 ROM に対して実行しない。
2. **逆アセンブルしない。** 逆アセンブラを公式 ROM に対して実行しない。
   他人が公開している PC-88 ROM の逆アセンブルリスト・解析記事の
   *コード断片* も読まない（仕様の散文説明は可）。
3. **`private/` の中身をリポジトリに持ち出さない。**
   `private/` 配下のファイル内容を、コミット対象ファイル・コミットメッセージ・
   ログ・コメントに転記しない。
4. **ROM 由来のバイト列を含むファイルをコミットしない。**
   データテーブルはルールから生成するか、挙動から再構成する。ダンプしない。

## やってよいこと

- ハードウェア資料（回路図、ポート仕様、技術書）を読む
- N88-BASIC の**マニュアル**（言語仕様・命令の意味）を読む
- GW-BASIC の MIT 公開ソースを読む
- エミュレータに計測フックを入れ、入出力の対応を**測定**する
- 測定結果から `docs/spec/` に仕様書を起こす
- `docs/spec/` の仕様書だけを見て実装する

## 資料の使い方 — **測定で裏が取れる種類の情報だけを採る**

技術書や解析記事には、ハードウェアの事実と ROM の解析結果が混ざって載っている。
**「書籍だから安全」ではない。分かれ目は、何が書いてあるかである。**

| 種類 | 例 | 扱い |
|---|---|---|
| (a) ハードウェアの事実 | ポート番号、ビットの意味、CRTC のコマンド書式、回路 | **読む** |
| (b) ROM 内部の構造 | エントリアドレス、ジャンプテーブル、ワークエリア番地、データテーブル | **読まない** |
| (c) コードそのもの | 逆アセンブルリスト、コード断片 | **読まない**（禁止事項2） |

判定は**測定で裏が取れるかどうか**でできる。

- (a) は測れば同じ値が出る。だから資料は**近道であって唯一の出所ではない**。
  実例: `IN 40` の bit5 = VRTC も、`C1`/`C3` の 8251 初期化手順も、
  測定と書籍が独立に一致した
- (b) は測っても「その資料と一致した」以上のことが言えない。**出所がその資料しかない。**

### 「自分で解析していないから安全」は逆である

**自分で測るほうが白い。** *Sony v. Connectix* が守ったのは
「**自分で**リバースエンジニアリングして得た事実」であって、
第三者の解析物を読む行為ではない。他人の解析を読むと、
その人が正当な手段で得たかどうかをこちらが引き受けることになる。

そして実務上いちばん重いのは**由来の鎖**である。

```
公式ROM  →  誰かの解析  →  書籍・記事  →  この実装
```

**各段が適法でも、鎖はつながったままになる。** 争いになったときに問われるのは
各段の適法性ではなく「独立に作ったと言えるか」なので、
(b) を読むと `docs/PLAN.md` 第3節の主張そのものが弱くなる。

### 手順

1. **可否はカバー・まえがき・序文で判定する。** 目次から入ると、判定のために
   中身へ近づくことになる（`docs/notes/refs-machinego-not-opened.md`）
2. **EPUB のテキスト走査ではなく、PDF のページ画像で読む。**
   `pdftoppm -f N -l N -r 150 -png <pdf> <out>`。
   テキストを順に走査すると隣の節が視界に入る。
   **前回の汚染はまさにこれで起きた**（`docs/notes/contamination-2026-08-07.md`。
   「短く切れば安全」は成り立たない——表は1行が短い）
3. **読んだページ番号を `docs/notes/` に記録する。**
   どのページを見てどの仕様を書いたかが残ること自体が、プロセスの証拠になる
4. Web ページにはカバーもまえがきも無い。**使い捨ての判定係に先に見せて、
   可否だけ報告させる**（`docs/notes/refs-kiwi-ohta-kanji.md`）

### 資料の格付け

**測定 > 書籍 > 出典記載の無い Web。** 食い違ったら測定を採る。
実例: 1982 年の PC-Techknow 付録6 はパレットを3ビットと定義するが、
測定値は収まらない（アナログパレットは mkIISR 以降）。
**資料が古い機種を書いていることは珍しくない。**

## 情報の流れ（これ以外の経路を作らない）

```
公式ROM (private/)  ──[測定フックのみ]──▶  測定ログ  ──▶  docs/spec/  ──▶  src/
        ▲                                                                   │
        └────────── 直接読む経路は存在しない ──────────────────────────┘
```

実装者（人間でもエージェントでも）が見てよいのは `docs/spec/` から右側だけ。

## 汚染したとき

隠さない。以下を実行し、その事実をコミットメッセージに残す。

1. 見てしまった範囲を特定する
2. その担当（人間なら本人、エージェントならそのセッション）を該当箇所の実装から外す
3. 該当箇所は測定からやり直す。既存実装は破棄する

「たぶん大丈夫」で続けると、その一箇所のために全体の主張が崩れる。

## コミット規律

- **測定コミットが実装コミットに先行する。** 順序そのものが独立実装の証拠になる。
- 行き止まりを `git reset` で消さない。失敗した試行を残す。写経の履歴には失敗が無い。
- コミットメッセージに「どの測定を根拠にこう書いたか」を書く。
- コミット前に `git status` で `private/` 由来の混入が無いことを確認する。

## パスの扱い

私物（公式 ROM、テスト用イメージ）のパスは**環境変数経由でのみ**受け取る。
リポジトリ内のスクリプトに絶対パスを焼き込まない。

```
PC88_REF_ROM_DIR   公式ROMの置き場（private/ 配下を想定）
PC88_REF_DISK_DIR  参照用ディスクイメージの置き場
```

## 言語

回答・作業説明・コミットメッセージはすべて日本語。

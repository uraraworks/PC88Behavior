; src/ext_bank/bank0.asm — 拡張ROMバンク0(N88_0.ROM)。土台のみ、機能無し。
;
; docs/spec/ext-rom-bank.md 第0節「位置づけ(実装スコープ)」配置案C:
; 既存モジュールの配置は動かさず、新しい機能だけをバンクへ新設する方針。
; 今回はバンク切り替えの仕組みそのものを固める段階のため、各バンクの
; 中身は固定値を返す試験ルーチン1本だけ(自己検査用)。新しいBASICの
; 機能はまだ置かない。
;
; 窓(0x6000-0x7FFF)の先頭が試験エントリになる。絶対番地参照
; (LD HL,label 等)を使わずPC相対命令(JR等)だけで書いているため、
; アセンブル時のPC(このファイル単体では0始まり)がそのまま窓内の
; オフセットに一致する——0x6000を起点にORGし直す必要が無い。
; 将来、絶対番地参照が要る機能をバンクに置く場合はこの前提が崩れる
; (仕様未確定、ext-rom-bank.md 第3節項3・4——バンク内の配置規約は
; 公式ROMの中身を測っていないため定義できない)。
EXT_BANK0_TEST_ENTRY:
    LD A,0xB0
    RET

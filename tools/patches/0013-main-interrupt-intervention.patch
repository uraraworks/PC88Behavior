diff --git a/Makefile.common b/Makefile.common
--- a/Makefile.common
+++ b/Makefile.common
@@ -60,6 +60,7 @@ SOURCES_C := $(CORE_DIR)/quasi88.c \
    $(CORE_DIR)/q88h_iolog.c \
    $(CORE_DIR)/q88h_exchange_intervention.c \
    $(CORE_DIR)/q88h_sub_interrupt_intervention.c \
+   $(CORE_DIR)/q88h_main_interrupt_intervention.c \
    $(CORE_DIR)/q88h_intlog.c \
    $(CORE_DIR)/q88h_fontsrc.c \
    $(CORE_DIR)/q88h_screenshot.c \
diff --git a/src/z80.c b/src/z80.c
--- a/src/z80.c
+++ b/src/z80.c
@@ -97,5 +97,6 @@
 #include "q88h_intlog.h"
 #include "q88h_sub_interrupt_intervention.h"
+#include "q88h_main_interrupt_intervention.h"
 
 
 #define S_FLAG      (0x80)
@@ -733,9 +734,15 @@ static	void	z80_interrupt( z80arch *z80 )
   if( z80 == &z80sub_cpu && q88h_sub_interrupt_intervention_before_ack() ){
     return;
   }
+  if( z80 == &z80main_cpu && q88h_main_interrupt_intervention_before_ack() ){
+    return;
+  }
 
   level = (z80->intr_ack)();
   if( z80 == &z80sub_cpu ){
     q88h_sub_interrupt_intervention_ack_result( level >= 0 );
   }
+  if( z80 == &z80main_cpu ){
+    q88h_main_interrupt_intervention_ack_result( level >= 0 );
+  }
   if( level >= 0 ){
